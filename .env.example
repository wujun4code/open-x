# Environment variables for Docker Compose deployment
# Copy this file to .env and customize the values

# ======================
# Security (REQUIRED)
# ======================
# Generate a strong random secret for JWT tokens
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production-please

# ======================
# URLs (REQUIRED)
# ======================
# Frontend URL - used by backend for CORS
# For local: http://localhost:3000
# For production: https://yourdomain.com
FRONTEND_URL=http://localhost:3000

# Backend API URL - used by frontend
# For local: http://localhost:4000/graphql
# For production: https://api.yourdomain.com/graphql
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql

# ======================
# Cloudflare R2 Storage (OPTIONAL)
# ======================
# Only needed if you want to enable media uploads
# Leave empty to disable media upload features
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=open-x-media
R2_PUBLIC_URL=
